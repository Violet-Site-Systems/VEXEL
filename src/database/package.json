{
  "name": "@vexel/database",
  "version": "1.0.0",
  "description": "Database module for VEXEL - PostgreSQL client and agent repository",
  "main": "../../dist/database/index.js",
  "types": "../../dist/database/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --config jest.config.js",
    "clean": "rm -rf ../../dist/database"
  },
  "keywords": [
    "vexel",
    "database",
    "postgresql"
  ],
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "@types/pg": "^8.11.10"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
